const assert = require('assert');
const request = require('supertest');
const server = require('../server');

describe('shop', function describeShop() {
  describe('get', function describeGet() {
    it('should return 403 when there is no longitude or latitude in the request', function() {

    });
  });
});